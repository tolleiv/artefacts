{
    "name": "artefacts",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "start": "node app",
        "test": "jasmine-node --verbose --color --captureExceptions spec"
    },
    "dependencies": {
        "express": "3.x",
        "request": "> 0.0.1",
        "persist": ">= 0.0.1",
        "sqlite3": ">= 0.0.1",
        "db-migrate": ">=0.0.3",
        "async": ">=0.0.3"
    },
    "devDependencies": {
        "jasmine-node": ">= 0.0.1"
    },
    "repository": {
        "type": "git",
        "url": "http://github.com/tolleiv/artefacts.git"
    }

}
